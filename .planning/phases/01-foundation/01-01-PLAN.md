---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Python development environment with wallet-agnostic configuration system.

Purpose: Establish the project structure and config patterns that all other components depend on.
Output: Working Python environment with requirements.txt, config.py, and .env template.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure with virtual environment</name>
  <files>requirements.txt, src/__init__.py, src/config.py, src/utils/__init__.py, .gitignore</files>
  <action>
    1. Create Python virtual environment: python3 -m venv venv
    2. Create project structure:
       - src/ (main source code)
       - src/utils/ (utility modules)
       - tests/ (test files)
       - logs/ (log output)
       - data/ (CSV exports)
    3. Create requirements.txt with dependencies:
       - py-clob-client (Polymarket API)
       - python-dotenv (env loading)
       - aiohttp (async HTTP)
       - web3 (Polygon interaction)
       - discord-webhook (notifications)
       - rich (terminal dashboard)
    4. Create .gitignore:
       - venv/, __pycache__/, .env, *.pyc, logs/, data/*.csv
       - DO NOT ignore .env.example
    5. Install dependencies: pip install -r requirements.txt
  </action>
  <verify>
    - ls -la shows venv/, src/, tests/, logs/, data/
    - pip list shows all required packages installed
    - python -c "import dotenv; import aiohttp; print('OK')" succeeds
  </verify>
  <done>Virtual environment active, all dependencies installed, project structure created</done>
</task>

<task type="auto">
  <name>Task 2: Create wallet-agnostic configuration system</name>
  <files>src/config.py, .env.example</files>
  <action>
    1. Create src/config.py:
       - Load from .env using python-dotenv
       - WALLET_PRIVATE_KEY (for easy test/main switching)
       - POLYGON_RPC_URL (default: public Polygon RPC)
       - Trading parameters: MAX_TOTAL_COST, MAX_TRADE_COST, MIN_TRADE_COST
       - Risk parameters: SAFETY_PAIR_COST, MAX_IMBALANCE, MAX_POSITION_SHARES
       - Strategy parameters: GRID_LEVELS=1, GRID_SPACING, TARGET_LOCKED_PROFIT
       - Network config: PRIMARY_WIFI, BACKUP_WIFI_1, BACKUP_WIFI_2, NETWORK_POLL_INTERVAL=15
       - Discord config: DISCORD_WEBHOOK_PNL, DISCORD_WEBHOOK_LOSSES, DISCORD_WEBHOOK_OUTAGES, DISCORD_USER_ID
       - DRY_RUN_MODE=True (default to paper trading)
       - Include validation function to check required vars exist
    2. Create .env.example with all variables (placeholder values):
       - Clear comments explaining each variable
       - Group by category (wallet, trading, risk, network, discord)
       - Mark which are required vs optional
    3. Add type hints and docstrings for beginner clarity
  </action>
  <verify>
    - python -c "from src.config import Config; c = Config(); print(c)" runs without error (with .env or defaults)
    - .env.example contains all documented variables
    - Config class validates required fields and raises clear errors for missing vars
  </verify>
  <done>Config loads from .env, all parameters documented, validation works, switching wallet is single env change</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `source venv/bin/activate` works
- [ ] `pip list` shows all required packages
- [ ] `python -c "from src.config import Config"` imports without error
- [ ] `.env.example` exists with all documented variables
- [ ] `.gitignore` excludes .env but not .env.example
</verification>

<success_criteria>
- Virtual environment created and activated
- All dependencies installed
- Config system loads environment variables
- Wallet can be switched by changing single .env value
- Project structure follows Python best practices
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
